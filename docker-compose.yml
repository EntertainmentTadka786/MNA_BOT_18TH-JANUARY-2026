version: '3.8'

services:
  telegram-bot:
    build: .
    ports:
      - "8080:8080"
    environment:
      - BOT_TOKEN=8083858449:AAHg-B6wzXmyshFzB1D4VKEYAAKfol4BV0Y
      - OWNER_ID=1080317415
      - GROUP_ID=-1003083386043
      - RENDER=true
      - PORT=8080
    volumes:
      - ./users.json:/var/www/html/users.json
      - ./movies.csv:/var/www/html/movies.csv
      - ./bot_log.txt:/var/www/html/bot_log.txt
      - ./error.log:/var/www/html/error.log
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080"]
      interval: 30s
      timeout: 10s
      retries: 3
