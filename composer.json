{
    "name": "telegram-forward-bot",
    "description": "Telegram Multi-Channel Forwarding Bot",
    "type": "project",
    "require": {
        "php": ">=8.0"
    },
    "autoload": {
        "psr-4": {
            "App\\": "src/"
        }
    },
    "scripts": {
        "post-install-cmd": [
            "chmod -R 777 users.json movies.csv bot_log.txt error.log"
        ]
    }
}